name: CI/CD Pipeline

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build-and-push:
    name: 🧱 Build & Push Docker Images
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout source code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Setup Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3️⃣ Login Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 4️⃣ Build & push api-gateway
      - name: Build & push api-gateway
        uses: docker/build-push-action@v6
        with:
          context: ./api-gateway
          file: ./api-gateway/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/api-gateway:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # 5️⃣ Build & push auth
      - name: Build & push auth
        uses: docker/build-push-action@v6
        with:
          context: ./auth
          file: ./auth/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/auth:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # 6️⃣ Build & push product
      - name: Build & push product
        uses: docker/build-push-action@v6
        with:
          context: ./product
          file: ./product/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/product:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # 7️⃣ Build & push order
      - name: Build & push order
        uses: docker/build-push-action@v6
        with:
          context: ./order
          file: ./order/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/order:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: 🚀 Deploy on Localhost (Windows Runner)
    needs: build-and-push
    runs-on: self-hosted

    steps:
      # 1️⃣ Kiểm tra Docker environment
      - name: Check Docker environment
        run: |
          echo "🔍 Checking Docker environment..."
          docker --version
          docker-compose --version
          docker ps

      # 2️⃣ Login to Docker Hub trên Windows runner
      - name: Login to Docker Hub on Windows
        run: |
          docker login -u $env:DOCKER_USERNAME -p $env:DOCKER_PASSWORD
          echo "✅ Logged in to Docker Hub successfully"
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}

      # 3️⃣ Dừng và xóa containers cũ
      - name: Stop and remove old containers
        run: |
          echo "🛑 Stopping old containers..."
          cd "D:\HTTT18B\Nam4_1\LTHDV\EProject-Phase-1"
          docker-compose down --remove-orphans --volumes || echo "No containers to stop"

      # 4️⃣ Pull images mới nhất từ Docker Hub
      - name: Pull latest Docker images
        run: |
          echo "📥 Pulling latest Docker images..."
          docker pull ${{ secrets.DOCKER_USERNAME }}/api-gateway:latest || echo "⚠️ Failed to pull api-gateway"
          docker pull ${{ secrets.DOCKER_USERNAME }}/auth:latest || echo "⚠️ Failed to pull auth"
          docker pull ${{ secrets.DOCKER_USERNAME }}/product:latest || echo "⚠️ Failed to pull product"
          docker pull ${{ secrets.DOCKER_USERNAME }}/order:latest || echo "⚠️ Failed to pull order"
          echo "✅ All images pulled successfully"

      # 5️⃣ Deploy với docker-compose (sử dụng images mới nhất)
      - name: Deploy with docker-compose
        run: |
          echo "🚀 Deploying with docker-compose..."
          cd "D:\HTTT18B\Nam4_1\LTHDV\EProject-Phase-1"
          docker-compose up -d --remove-orphans --force-recreate
          echo "✅ Deployment completed"

      # 6️⃣ Kiểm tra trạng thái deployment
      - name: Verify deployment status
        run: |
          echo "📊 Deployment Status:"
          echo "======================"
          docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
          echo "======================"
          
          # Kiểm tra các containers đang chạy
          running_containers=$(docker ps --format "{{.Names}}" | wc -l)
          echo "📈 Running containers: $running_containers"
          
          if [ $running_containers -gt 0 ]; then
            echo "✅ Deployment successful - $running_containers containers are running"
          else
            echo "❌ Deployment failed - No containers running"
            exit 1
          fi

      # 7️⃣ Kiểm tra health của services (sau 30 giây)
      - name: Check services health
        run: |
          echo "🏥 Checking services health..."
          timeout 90 bash -c '
            while true; do
              healthy_count=$(docker ps --filter "health=healthy" --format "{{.Names}}" | wc -l)
              if [ $healthy_count -gt 0 ]; then
                echo "✅ $healthy_count services are healthy"
                break
              else
                echo "⏳ Waiting for services to become healthy..."
                sleep 10
              fi
            done
          ' || echo "⚠️ Some services may not have health checks"

      # 8️⃣ Dọn dẹp Docker resources không sử dụng
      - name: Clean up unused Docker resources
        run: |
          echo "🧹 Cleaning up unused Docker resources..."
          docker image prune -f
          docker container prune -f
          echo "✅ Cleanup completed"

      # 9️⃣ Thông báo deployment hoàn tất
      - name: Deployment summary
        run: |
          echo "🎉 DEPLOYMENT COMPLETED SUCCESSFULLY!"
          echo "======================================"
          echo "📦 Services deployed:"
          docker ps --format "• {{.Names}} ({{.Status}})"
          echo "======================================"